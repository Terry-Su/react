const path = require('path')

const OUTPUT_FOLDER_NAME = 'build-16.8.4';
const OUTPUT_FOLDER = path.resolve( __dirname, OUTPUT_FOLDER_NAME );

module.exports = {
  OUTPUT_FOLDER_NAME,
  OUTPUT_FOLDER,
};
